@if (Visible)
{
    <div class="modal-backdrop" @onclick="OnCancel">
        <div class="modal" style="max-width: 380px;" @onclick:stopPropagation="true" role="dialog" aria-labelledby="modal-title-confirm-delete">
            <div class="modal-header" style="padding: 0.875rem 1.25rem;">
                <h3 class="modal-title" id="modal-title-confirm-delete">Confirm Delete</h3>
                <button class="modal-close" @onclick="OnCancel">&times;</button>
            </div>
            <div class="modal-body" style="padding: 1rem 1.25rem;">
                <p style="color: var(--text-primary); margin: 0;">@Message</p>
            </div>
            <div class="modal-footer" style="padding: 0.75rem 1.25rem;">
                <button class="btn btn-secondary" @onclick="OnCancel">Cancel</button>
                <button class="btn btn-danger" @onclick="OnConfirm">Delete</button>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public bool Visible { get; set; }
    [Parameter] public string Message { get; set; } = "Are you sure you want to delete this item?";
    [Parameter] public EventCallback OnConfirm { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
}
